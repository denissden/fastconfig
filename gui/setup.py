import setuptools


setuptools.setup(
    name="fastconfiggui",
    version="1.0",
    author="denissden",
    packages=["fastconfiggui"],
    install_requires=[
        "setuptools",
        "requests"
    ]
)